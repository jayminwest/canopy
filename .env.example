# canopy — environment configuration
#
# canopy does not read environment variables at runtime. This file exists
# so contributors know the convention applies: copy `.env.example` to
# `.env` if/when canopy starts reading env vars. `.env` itself is
# gitignored; `.env.example` is committed and tracked.
